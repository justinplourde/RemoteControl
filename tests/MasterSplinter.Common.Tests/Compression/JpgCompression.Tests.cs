using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace MasterSplinter.Common.Tests.Compression
{
    [TestClass]
    public class JpgCompressionTests
    {
        [TestMethod, TestCategory("Compression")]
        [Ignore("JPG compression remains in the legacy Windows remote desktop surface until it is extracted into a reviewed platform-specific project.")]
        public void CompressionTest()
        {
            Assert.Inconclusive();
        }
    }
}
